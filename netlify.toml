[build]
  base = "apps/web"
  publish = "apps/web/dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "npm run dev"
  port = 5174
  publish = "apps/web/dist"
